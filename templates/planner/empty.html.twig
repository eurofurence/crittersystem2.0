{% extends 'base.html.twig' %}
{% import 'components/data/_macros.twig' as d %}

{% block title %}{{ 'planner.page.title'|trans }}{% endblock %}

{% block body %}
  {{ d.page_header('planner.page.title'|trans) }}
  {{ d.empty_state('planner.empty.no_departments.title'|trans, {
    message: 'planner.empty.no_departments.message'|trans,
    actionLabel: 'planner.empty.no_departments.action'|trans,
    actionHref: path('app_manage_department_index')
  }) }}
{% endblock %}
