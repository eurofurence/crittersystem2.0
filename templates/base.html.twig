<!DOCTYPE html>
<html lang="en" data-bs-theme="{{ active_theme.type }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
        <title>{% block title %}VMS{% endblock %}</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}

        {# Theme stylesheet — loaded LAST so its `--tblr-*` / `--bs-*` overrides win the cascade. #}
        <link rel="stylesheet" href="{{ asset(active_theme.assetPath) }}">
    </head>
    <body>
        {# COMPONENTS #}
        {% import 'components/navigation/_macros.twig' as nav %}
        {% import 'components/notification/_macros.twig' as n %}
        {% import 'components/icon/_macros.twig' as i %}

        {% block navbar %}
        <div class="page">
            {# ===================== TOP NAVBAR (brand + user menu) ===================== #}
            <header class="navbar navbar-expand-md d-print-none">
                <div class="container-xl">
                    <button class="navbar-toggler" type="button"
                            data-bs-toggle="collapse" data-bs-target="#navbar-menu"
                            aria-controls="navbar-menu" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <a href="{{ path('app_home') }}"
                       class="navbar-brand navbar-brand-autodark d-none-navbar-horizontal pe-0 pe-md-3 fw-bold fs-3">
                        VMS
                    </a>

                    <div class="navbar-nav flex-row order-md-last">
                        {% if app.user %}
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link d-flex lh-1 p-0 px-2" data-bs-toggle="dropdown"
                                   aria-label="Open user menu" aria-expanded="false">
                                    <span class="avatar avatar-sm">{{ app.user.userIdentifier|slice(0, 2)|upper }}</span>
                                    <div class="d-none d-xl-block ps-2">
                                        <div>{{ app.user.userIdentifier }}</div>
                                        <div class="mt-1 small text-secondary">Signed in</div>
                                    </div>
                                </a>
                                <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                                    <a href="{{ path('app_settings') }}" class="dropdown-item">
                                        {{ i.icon('settings', {class: 'icon-1 me-2 text-muted'}) }}Settings
                                    </a>
                                    <a href="{{ path('app_digital_id') }}" class="dropdown-item">
                                        {{ i.icon('id', {class: 'icon-1 me-2 text-muted'}) }}Digital ID
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <a href="{{ path('app_logout') }}" class="dropdown-item">
                                        {{ i.icon('logout', {class: 'icon-1 me-2 text-muted'}) }}Logout
                                    </a>
                                </div>
                            </div>
                        {% else %}
                            <div class="nav-item">
                                <a href="{{ path('app_login') }}" class="btn btn-primary">
                                    {{ i.icon('login', {class: 'icon-2'}) }}Login
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </header>

            {# ===================== HORIZONTAL MENU ===================== #}
            <header class="navbar-expand-md d-print-none">
                <div class="collapse navbar-collapse" id="navbar-menu">
                    <div class="navbar">
                        <div class="container-xl">
                            <ul class="navbar-nav">
                                {{ nav.nav_item('Home', 'app_home', {icon: 'home'}) }}
                                {{ nav.nav_item('Dashboard', 'app_dashboard', {icon: 'dashboard'}) }}
                                {% if app.user %}
                                    {% if is_granted('news') %}{{ nav.nav_item('News', 'app_news_index', {icon: 'news', exact: false}) }}{% endif %}
                                    {{ nav.nav_item('Shifts', 'app_shift_index', {icon: 'clock', exact: false}) }}
                                    {{ nav.nav_item('My Shifts', 'app_my_shifts', {icon: 'checklist'}) }}
                                    {{ nav.nav_item('Volunteer Types', 'app_membership_index', {icon: 'puzzle'}) }}
                                    {% if is_granted('user_messages') %}{{ nav.nav_item('Messages', 'app_messages_index', {icon: 'mail', exact: false}) }}{% endif %}
                                    {% if is_granted('faq.view') %}{{ nav.nav_item('FAQ', 'app_faq_index', {icon: 'help'}) }}{% endif %}
                                    {{ nav.nav_item('Certifications', 'app_certifications_index', {icon: 'award'}) }}
                                    {% if is_granted('user.type.staff') %}{{ nav.nav_item('Staff', 'app_staff_overview', {icon: 'shield', exact: false}) }}{% endif %}
                                    {% if is_granted('backstage.view') %}{{ nav.nav_item('Backstage', 'app_backstage', {icon: 'gift', exact: false}) }}{% endif %}
                                {% endif %}
                                {% if app.user and (is_granted('admin_rooms') or is_granted('admin_volunteer_types') or is_granted('admin_shifts') or is_granted('admin') or is_granted('admin_event_config')) %}
                                    {{ nav.nav_item('Manage', 'app_manage', {icon: 'tools', exact: false}) }}
                                {% endif %}
                                {{ nav.nav_item('Navigation Kit', 'app_navigation_kit', {icon: 'compass'}) }}
                                {{ nav.nav_item('Theme Kit', 'app_theme_kit', {icon: 'palette'}) }}
                            </ul>
                        </div>
                    </div>
                </div>
            </header>

            {# ===================== PAGE CONTENT ===================== #}
            <div class="page-wrapper">
                <div class="page-body">
                    <div class="container-xl">
                        {# FLASH MESSAGES #}
                        {{ n.flash_messages(app) }}

                        {% block body %}{% endblock %}
                    </div>
                </div>
            </div>
        </div>
        {% endblock %}
    </body>
</html>
