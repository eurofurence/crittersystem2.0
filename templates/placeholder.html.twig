{% extends 'base.html.twig' %}
{% import 'components/data/_macros.twig' as d %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
  {{ d.page_header(title, {subtitle: subtitle|default('')}) }}
  {{ d.empty_state('system.placeholder.title'|trans, {message: 'system.placeholder.message'|trans}) }}
{% endblock %}
