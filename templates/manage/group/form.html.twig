{% extends 'base.html.twig' %}
{% import 'components/data/_macros.twig' as d %}
{% import 'components/forms/_macros.twig' as f %}

{% block title %}{{ heading }}{% endblock %}

{% block body %}
  {{ d.page_header(heading, {
    actions: [{label: '← Back', href: path('app_manage_group_index'), class: 'btn btn-outline-secondary'}]
  }) }}

  {{ form_start(form) }}
    <div class="row g-3">
      {{ f.field(form.name, {col: 'col-12 col-md-6'}) }}
      {{ f.field(form.role, {col: 'col-12 col-md-6'}) }}
    </div>

    {# Group the permission checkboxes by their domain category. #}
    {% set cats = {} %}
    {% for child in form.privileges %}
      {% set cat = permissionMeta[child.vars.value].category|default('Other') %}
      {% set cats = cats|merge({ (cat): (cats[cat]|default([]))|merge([child]) }) %}
    {% endfor %}

    <div class="mt-3">
      <div class="form-label">Permissions</div>
      <div class="row g-3">
        {% for cat, children in cats %}
          <div class="col-12 col-lg-6">
            <div class="card h-100">
              <div class="card-header py-2"><strong>{{ cat }}</strong></div>
              <div class="card-body py-2">
                {% for child in children %}
                  {% set name = child.vars.value %}
                  <label class="form-check">
                    {{ form_widget(child) }}
                    <span class="form-check-label">
                      <code>{{ child.vars.label }}</code>
                      {% if permissionMeta[name].twoFactor %}
                        <span class="badge text-bg-purple ms-1" title="Requires step-up authentication">2FA</span>
                      {% endif %}
                      <span class="d-block text-muted small">{{ permissionMeta[name].description }}</span>
                    </span>
                  </label>
                {% endfor %}
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
      {{ form_errors(form.privileges) }}
    </div>

    <div class="mt-3">
      <button class="btn btn-primary">Save</button>
      <a class="btn btn-link" href="{{ path('app_manage_group_index') }}">Cancel</a>
    </div>
  {{ form_end(form) }}
{% endblock %}
