{% extends 'base.html.twig' %}
{% import 'components/forms/_macros.twig' as f %}
{% import 'components/data/_macros.twig' as d %}

{% block title %}{{ heading|trans }}{% endblock %}

{% block body %}
  {{ d.page_header(heading|trans, {
    actions: [{label: 'manage.news.action.back'|trans, href: path('app_manage_news_index'), class: 'btn btn-outline-secondary'}]
  }) }}

  {{ form_start(form) }}
    <div class="row">
      {{ f.field(form.title, {col: 'col-12'}) }}
      {{ f.field(form.text, {col: 'col-12'}) }}
    </div>
    <div class="row">
      {{ f.check(form.isPinned) }}
      {{ f.check(form.isHighlighted) }}
      {{ f.check(form.isMeeting) }}
      {{ f.check(form.staffOnly) }}
    </div>
    {% if showNotify %}
      <div class="form-check mt-2">
        <input class="form-check-input" type="checkbox" name="notify" value="1" id="notify">
        <label class="form-check-label" for="notify">{{ 'manage.news.field.notify.label'|trans }}</label>
      </div>
    {% endif %}
    <button class="btn btn-primary mt-3">{{ 'common.action.save'|trans }}</button>
  {{ form_end(form) }}
{% endblock %}
