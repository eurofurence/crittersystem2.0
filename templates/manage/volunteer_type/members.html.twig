{% extends 'base.html.twig' %}
{% import 'components/data/_macros.twig' as d %}

{% block title %}{{ 'manage.volunteer_type.members.page_title'|trans({'%name%': type.name}) }}{% endblock %}

{% block body %}
  {{ d.page_header('manage.volunteer_type.members.heading'|trans({'%name%': type.name}), {
    subtitle: 'manage.volunteer_type.members.subtitle'|trans,
    actions: [{label: 'manage.volunteer_type.members.back'|trans, href: path('app_manage_volunteer_type_index'), class: 'btn btn-outline-secondary'}]
  }) }}

  {{ include('manage/volunteer_type/_manage_members.html.twig') }}
{% endblock %}
