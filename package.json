{
    "name": "critter",
    "private": true,
    "type": "module",
    "comment": "TEST TOOLING ONLY. Assets are served by Symfony AssetMapper straight from assets/ with no bundling, and that must stay true: nothing here may become a build step for shipped JavaScript. These packages exist so Stimulus controllers can be executed in tests instead of only rendered as markup. @hotwired/stimulus is pinned to the version in importmap.php - if you change one, change both.",
    "scripts": {
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "@hotwired/stimulus": "3.2.2",
        "happy-dom": "^15.11.0",
        "vitest": "^2.1.0"
    },
    "allowScripts": {
        "esbuild@0.21.5": true
    }
}
